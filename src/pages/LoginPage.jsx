import React from 'react';
import LoginBox from './LoginBox'; // Importing the LoginBox component

const LoginPage = () => {
  return (
    <div className="hold-transition login-page">
      <LoginBox />
    </div>
  );
};

export default LoginPage;
